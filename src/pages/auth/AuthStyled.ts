import styled from "styled-components";

export const WrapperAuth = styled.div`
  .auth-error {
    color: red;
  }

  .title-auth {
    font-size: 40px;
    text-align: center;
    padding: 20px 0px;
  }

  .label-auth {
    padding-bottom: 5px;
  }
`;
